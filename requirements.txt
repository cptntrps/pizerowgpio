# Pi Zero 2W Medicine Tracker - Python Dependencies
# Install with: pip3 install -r requirements.txt

# Web Framework (updated to 3.0+ for security patches)
Flask>=3.0.0,<4.0.0

# Image Processing (updated to 10.0+ for security patches)
Pillow>=10.0.0,<11.0.0

# GPIO Control (updated version)
gpiozero>=2.0.0,<3.0.0

# Data Validation (updated version)
marshmallow>=3.20.0,<4.0.0

# HTTP Client Library
requests>=2.31.0,<3.0.0

# Security & Configuration Management
Flask-Talisman>=1.1.0  # Security headers
Flask-Limiter>=3.5.0   # Rate limiting
Flask-CORS>=4.0.0      # CORS handling
Flask-JWT-Extended>=4.5.0  # JWT authentication
python-dotenv>=1.0.0   # Environment variables management

# Database (built-in with Python 3)
# sqlite3

# Standard library dependencies (no installation needed):
# - json
# - threading
# - subprocess
# - datetime
# - logging
